Merci pour l'intérêt que vous portez à {{ application.name }}.

pour finaliser votre inscription, veuillez cliquer sur le lien ci-dessous :

{{ url('identity_and_access_confirm_account', {token: token}) }}
